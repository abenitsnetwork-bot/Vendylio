# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> ⚠️ **Do NOT run `/init` on this project.** This CLAUDE.md is shipped with the starter and contains battle-tested invariants (runtime=nodejs enforcement, protected file list, OAuth refusal of `email_verified=false`, advisory-lock withdrawals, outbox pattern, raw-body HMAC ordering, …). Running `/init` would regenerate this file from the codebase and erase those invariants. Claude Code already loads this file automatically at session start — no command needed.

## What this project is

**A v1-shipped, headless Next.js 16 monolith starter.** Single full-stack app (App Router API Route Handlers + Server Actions + Prisma 5 + Neon + Upstash + Cloudinary + Resend + Stripe/Stripe Connect + DoorDash Drive + Uber Direct + Sentry). There is no separate Express backend anymore — server logic lives under `frontend/src/app/api/*` and `frontend/src/lib/server/*`. The app **ships only logic** — no UI components — so each fork designs its own UX.

Origin: bootstrapped from `amadou-template` (the legacy monorepo predecessor) on 2026-05-07; the port to a single Next.js 16 app shipped through 7 phases (auth → OAuth/notifs → admin → uploads/withdrawals → webhooks/cron → docs/tests → final pass). 555/555 unit tests green (the storage swap dropped the now-obsolete `/api/files/[...key]` proxy tests).

**For an AI agent picking up this repo:** the architecture sections below describe what's already been built. Anything not listed under "Files Claude must NOT modify" is fair game to extend, refactor, or replace per your fork's needs — that's the point of a starter. The protected list is the small set of files where the invariants are subtle (refresh-token races, HMAC integrity, advisory locks…); everything else is the fork's surface area.

**Beginner workflow (vibe coding)** — clone, plug a Neon `DATABASE_URL`, open Claude Code, describe what you want, ship. See [WORKFLOW.md](WORKFLOW.md). The starter ships:
- [.mcp.json](.mcp.json) — empty MCP server map by default. Banani is optional; if the user wants it, the `setup-kit` skill walks through pasting their MCP connection block.
- [.planning/features.json](.planning/features.json) — machine-readable manifest of the optional surfaces (payments, oauth-google, uploads-cloudinary, email-resend, admin-backoffice, multi-tenancy, …) — declares what each surface needs so manual pruning per [PRUNING.md](PRUNING.md) stays safe.
- GSD (`get-shit-done-cc`) is **not** a prerequisite. It's an optional level-up workflow surfaced after a beginner's first feature, not by default.

Read [README.md](README.md) for the public-facing contract (endpoints, env vars, design swap, deploy) and [STATUS.md](STATUS.md) for the historical port roadmap. Reference pages live in [examples/frontend-pages/](examples/frontend-pages/) — copy/restyle freely, they all consume the same `/api/*` JSON contract.

## Commands

pnpm workspace — run from repo root unless noted. The root `package.json` is a thin orchestrator: every script delegates to `pnpm --filter frontend run X` (the workspace currently has a single package, but the root layer is preserved as an architectural seam).

| Task | Command |
|---|---|
| Dev (Next.js on :3000, Turbopack) | `pnpm dev` |
| Build | `pnpm build` |
| Apply Prisma schema (dev iteration) | `pnpm db:push` |
| Versioned migrations | `pnpm db:migrate:dev` (local) / `pnpm db:migrate:deploy` (CI/prod) |
| Migration status | `pnpm db:migrate:status` |
| Open Prisma Studio (:5555) | `pnpm db:studio` |
| Bootstrap first SUPERADMIN | `pnpm db:make-superadmin <email>` |
| Unit tests (Vitest) | `pnpm test` |
| Single test file | `pnpm --filter frontend exec vitest run src/lib/server/<file>.test.ts` |
| Single test by name | `pnpm --filter frontend exec vitest run -t "<test name>"` |
| Watch one test | `pnpm --filter frontend exec vitest src/lib/server/<file>.test.ts` |
| Typecheck | `pnpm typecheck` |
| Lint | `pnpm lint` |
| Format | `pnpm format` (or `pnpm format:check`) |

Integration tests are deferred (no formal harness in v1) — `pnpm smoke:auth` provides a manual UAT script for the auth happy path against a running `pnpm dev`. See README.

`pnpm dev` auto-wipes `.next` first (a `predev` hook) — do not remove this. `next build` (Webpack) and `next dev --turbopack` write incompatible manifest formats into the same `.next` folder; running a local `pnpm build` and then `pnpm dev` without clearing it in between corrupts Turbopack's route manifest and makes API routes 404 at random until `.next` is deleted. The hook only touches the local dev workflow — Vercel's own `next build` is a fresh checkout with no stale `.next`, so this has no effect on deploys.

**Before committing:** `pnpm format && pnpm lint && pnpm typecheck && pnpm test` — must all pass.

## High-level architecture

**Single Next.js 16 App Router app** at `frontend/`. The root `package.json` keeps the pnpm-workspace shell so a future package (e.g. shared types) can be added without re-plumbing scripts. There is no `backend/` package — all server code is colocated under `frontend/src/`.

**Boot flow** — Next.js owns the server lifecycle, so there is no hand-rolled middleware chain. Three things matter:
1. **`frontend/instrumentation.ts`** is Next's `register()` hook — Sentry inits here (server + edge via `sentry.{server,edge}.config.ts`; client via `sentry.client.config.ts`). No DSN env → silent no-op.
2. **Every Route Handler MUST `export const runtime = 'nodejs'`** (Prisma + bcrypt + raw-body needs). [frontend/src/lib/server/observability/runtime-enforcement.test.ts](frontend/src/lib/server/observability/runtime-enforcement.test.ts) walks `app/api/**/route.ts` and fails CI if any route forgets it.
3. **Per-request observability** flows through [frontend/src/lib/server/observability/request-context.ts](frontend/src/lib/server/observability/request-context.ts) (`makeRequestContext` + `withRequestContext` + scoped `log`). Handlers wrap their body in `withRequestContext()` so logs auto-attach `requestId` / `userId` / `route`.

**Optional providers boot conditionally.** If `CLOUDINARY_*` / `RESEND_*` / `STRIPE_*` / `DOORDASH_*` / `UBER_DIRECT_*` / `GOOGLE_*` envs are absent, the corresponding routes either 404 silently or return 503 (e.g., `/api/upload` → 503 STORAGE_NOT_CONFIGURED when Cloudinary creds are missing). `frontend/src/lib/server/redis.ts` exposes `redis: Redis | null` (returns `null` rather than throwing when env is missing — call sites decide fallback). The app still boots and `/api/auth` still works. `log.warn` announces which providers are inert.

**Auth model** ([frontend/src/lib/server/auth.ts](frontend/src/lib/server/auth.ts)): access JWT (15min, all paths) + refresh JWT (7d, scoped to `/api/auth` for blast-radius reduction) + CSRF token (7d, double-submit cookie). All cookies are namespaced by `COOKIE_PREFIX` (default `app`) and set via `cookies()` from `next/headers` (async). All mutating endpoints require the `x-csrf-token` header echoed from the `<prefix>-csrf` cookie — `verifyCsrf(req)` returns a `NextResponse | null` you bail on at the top of each handler. **Signup is enumeration-resistant**: identical 201 response regardless of email existence, no cookies issued at signup — cookies are issued by `POST /verify-email` after the user enters their 8-char Crockford code. The verification email is written to the outbox in the signup tx (durable), then delivered immediately from `after()` via `sendVerificationCodeNow` ([frontend/src/lib/server/auth/send-verification-now.ts](frontend/src/lib/server/auth/send-verification-now.ts)) — best-effort, claims the outbox row so the 1-min `outbox-drain` cron doesn't double-send, releases it back to the cron on failure; runs post-response so signup latency + enumeration-timing parity are untouched. **`after` is imported from `next/server`, not `@vercel/functions`.** Per-email rate limits (login 10/15m, signup 5/h, etc.) sit on top of the global IP limiter via [frontend/src/lib/server/middleware/rate-limit-by-email.ts](frontend/src/lib/server/middleware/rate-limit-by-email.ts). **Every `/api/cron/*` route exports `GET` (`export const GET = POST`) — Vercel Cron invokes cron paths with GET; a POST-only handler 405s every tick and nothing async runs.**

**Middleware HOFs** ([frontend/src/lib/server/middleware/index.ts](frontend/src/lib/server/middleware/index.ts)) — `requireAuth` / `requireAdmin` / `requireSuperadmin` / `requireOrgRole` / `optionalAuth` each return `Context | NextResponse`. Pattern in handlers: `if (auth instanceof NextResponse) return auth;`.

**Frontend `api()` wrapper** ([frontend/src/lib/api.ts](frontend/src/lib/api.ts)): auto-refreshes on 401 with a single-flight lock, attaches CSRF, and **only retries `GET`/`HEAD` on network errors** — never mutating verbs (would risk duplicate charges/withdrawals). `ApiError.code` exposes the server's stable error code (e.g. `PIN_REQUIRED`, `INSUFFICIENT_BALANCE`) — switch on `.code`, not on `.message`.

**Webhook idempotency + outbox:** [frontend/src/lib/server/webhook/handler.ts](frontend/src/lib/server/webhook/handler.ts) returns `(NextRequest) => Promise<NextResponse>` and reads the raw body via `await req.arrayBuffer()` (preserves byte-identical HMAC). The handler runs a `Serializable` Prisma transaction with `WebhookLog @@unique([externalId, eventType])` for dedup. Side-effects (emails, notifications) must NOT run as a postCommit closure — they go to the **outbox** ([frontend/src/lib/server/outbox/](frontend/src/lib/server/outbox/)) inside the same tx, drained by a Vercel Cron route ([frontend/src/app/api/cron/outbox-drain/route.ts](frontend/src/app/api/cron/outbox-drain/route.ts), every 1 min).

**Withdrawals are race-free:** the route runs guards + PENDING insert inside a `Serializable` Prisma transaction guarded by `pg_advisory_xact_lock(hashtext(userId))` ([frontend/src/lib/server/withdrawals/lock.ts](frontend/src/lib/server/withdrawals/lock.ts)). Two concurrent attempts for the same user serialize on the lock, so the second one sees the first's PENDING reservation and is correctly rejected as `INSUFFICIENT_BALANCE`.

**Payout methods (Phase 2).** `Withdrawal.destination` is `{ method: 'CASH_APP', cashtag }` | `{ method: 'ZELLE', contact }` | `{ method: 'BANK' }`; `Withdrawal.provider` is `'manual'` (Cash App / Zelle — an operator sends the money by hand and closes the row via `POST /api/admin/withdrawals/[id]/complete`) or `'stripe_transfer'` (BANK). A `BANK` request is only accepted when the store's `stripeOnboardingStatus === 'ACTIVE'` (else `422 BANK_PAYOUT_UNAVAILABLE`) — no bank details are stored, Stripe already holds the seller's verified account from Connect onboarding. A SUPERADMIN fires it via **`POST /api/admin/withdrawals/[id]/send-transfer`** (semi-auto — no cron auto-sends): claim `PENDING→PROCESSING` under the lock, `stripe.transfers.create` (net of commission, idempotency key `wd-transfer-<id>`) outside the tx, then `PROCESSING→COMPLETED` + `providerPayoutId=tr_xxx`; a Stripe failure reverts to `FAILED` (audited `withdrawal.send_transfer[_failed]`). `transfer.reversed` on the `stripe-connect` webhook (now also subscribed to that event) flips a bounced payout back to `FAILED`. `createConnectTransfer` lives in [payments/stripe-connect.ts](frontend/src/lib/server/payments/stripe-connect.ts).

**Payments — Stripe + Stripe Connect** ([frontend/src/lib/server/payments/](frontend/src/lib/server/payments/), interface `PaymentProvider` in `provider.ts`). Card path: when the store's `stripeOnboardingStatus === 'ACTIVE'` + `stripeAccountId` set, `POST /api/orders` uses a **Stripe Connect destination charge** with the platform commission as the `application_fee_amount` (`payments/stripe-connect.ts`, `Order.provider = 'stripe_connect'`); otherwise it falls back to a **platform-account charge** (`payments/stripe.ts`, `Order.provider = 'stripe_platform'`) and the commission is settled via the manual withdrawal path. **Cash App / Zelle** are manual methods — no Stripe call, no `paymentUrl`, `Order.provider = 'cashapp_manual' | 'zelle_manual'`, confirmed by the seller via `POST /api/orders/[id]/confirm-payment`. **PAY-01 (reversed by Phase 1b): manual-method orders now record the real `commissionAmount` / `netAmount`** (`MANUAL_MONEY_PROVIDERS` in `markPaid.ts`) and accrue a `CommissionCharge` receivable — see the **Commission on Cash App / Zelle** section below. `computeBalance` ([withdrawals/balance.ts](frontend/src/lib/server/withdrawals/balance.ts), PROTECTED) still counts **only `stripe_platform`** orders (net of commission) minus withdrawals — `stripe_connect` money lands on the seller's own Stripe, manual-method money went straight to the seller — but the withdrawals route now wraps that computer to also subtract OWED commission. `GET /api/withdrawals` returns `availableCents` (base balance − owed commission) + `commissionOwedCents` for the Billing & Payouts page. A single in-memory `CircuitBreaker` ([payments/circuit-breaker.ts](frontend/src/lib/server/payments/circuit-breaker.ts), PROTECTED) guards charge calls. Stripe webhook: `/api/webhooks/stripe` via the PROTECTED `createWebhookHandler` factory (raw-body HMAC, Serializable tx, `WebhookLog` dedup), gates on `payment_status === 'paid'` **and** `amount_total === order.amount` before `applyOrderPaidEffects`. `charge.refunded` is not yet handled (Session id vs PaymentIntent id — seller-initiated refunds go through `POST /api/orders/[id]/refund`).

**Plans & billing — Free / Pro.** `Store.plan` (`FREE | PRO`, default `FREE`) is the single value read everywhere ([frontend/src/lib/server/plan/features.ts](frontend/src/lib/server/plan/features.ts) `planFeatures()` turns it into a concrete feature set; `requirePro(store, feature)` in [middleware/require-pro.ts](frontend/src/lib/server/middleware/require-pro.ts) is the 402 route guard; `usePlan()` in [frontend/src/lib/usePlan.ts](frontend/src/lib/usePlan.ts) is the client mirror, UI-only). **Phase 3 wired the gates** — all return **402 `PLAN_UPGRADE_REQUIRED`** (or `AI_QUOTA_EXCEEDED`) and gate only the enabling action, never breaking what a store already had before a downgrade: promo-code creation (`POST /api/discounts` → `requirePro`), courier delivery (`PATCH /api/stores/fulfillment` — the uberDirect/doordash OFF→ON transition), storefront hero-image cap (`PATCH /api/stores` — Free 1 / Pro 3), AI monthly quota (`POST /api/ai/generate-description` — Free 5/mo via [ai/monthly-quota.ts](frontend/src/lib/server/ai/monthly-quota.ts), Pro unlimited), the "Powered by Vendylio" storefront badge (`getPublicStore` → `showPoweredBy`), and plan-aware withdrawal limits (`POST /api/withdrawals` adjusts the guard config in-route — Pro: `cooldownHours=0` + daily ceiling ×5 — `guards.ts`/`balance.ts` untouched). `advancedAnalytics` + `teamMembers` shipped in **Phase 4a** (see the two sections below); `customDomain` is the one net-new Pro flag still unbuilt (Phase 4b — Vercel Domains API). The plan value is driven by six `Store` columns (`stripeCustomerId` — the merchant as a *customer of Vendylio*, distinct from `stripeAccountId`; `stripeSubscriptionId`, `subscriptionStatus`, `subscriptionCurrentPeriodEnd`, `planSource` = `SUBSCRIPTION | COMP | null`, `planCompExpiresAt`). Real Stripe **subscription** billing: `POST /api/billing/{checkout,portal,status}` ([frontend/src/lib/server/billing/stripe-billing.ts](frontend/src/lib/server/billing/stripe-billing.ts), inert → 503 `BILLING_NOT_CONFIGURED` without `STRIPE_PRO_PRICE_ID`), and a **separate** webhook `POST /api/webhooks/stripe-billing` (own secret `STRIPE_BILLING_WEBHOOK_SECRET`) on the PROTECTED `createWebhookHandler` factory — `syncSubscriptionFromStripe` ([billing/sync-subscription.ts](frontend/src/lib/server/billing/sync-subscription.ts)) maps `active/trialing → plan PRO/planSource SUBSCRIPTION`, `past_due → status only` (grace until period end), `canceled → plan FREE` (only when `planSource` was `SUBSCRIPTION`). `POST /api/stores/upgrade` is now a **410** (`USE_BILLING_CHECKOUT`). A SUPERADMIN can comp Pro (e.g. pilot) via `PATCH /api/admin/stores/[id]/plan` → `planSource='COMP'` + `planCompExpiresAt` (audited; refuses a store paying via Stripe). The `plan-downgrade-sweep` cron retires expired comps + lapsed past-due subs.

**Commission on Cash App / Zelle — universal commission (Phase 1b).** The platform now takes its marketplace cut on manual-method orders too. When the seller confirms a `cashapp_manual` / `zelle_manual` order (`markPaid.ts` → `applyOrderPaidEffects`), the real commission is computed like any card order **and** a `CommissionCharge` row (`status='OWED'`, `kind='SALE'`, `@@unique([orderId, kind])` idempotency) is written in the same Serializable tx — a **receivable**, because that P2P money went straight to the seller. It's collected two ways: (1) **withheld from the next withdrawal** — `POST /api/withdrawals` wraps `createDefaultBalanceComputer` ([commission/owed.ts](frontend/src/lib/server/commission/owed.ts) `owedCommissionCents` / `planCommissionSettlement`, FIFO) so the merchant enters the **net** amount they receive, `Withdrawal.amount` stores the **gross** debit (net + `commissionSettledCents`), and the settled charges flip `OWED → SETTLED` under the advisory lock; (2) **Stripe-invoiced** — a merchant with no withdrawable balance is billed on the card on file once their OWED total clears `COMMISSION_MIN_INVOICE_CENTS` (default 1000), by the `commission-settlement-sweep` cron ([billing/commission-settlement.ts](frontend/src/lib/server/billing/commission-settlement.ts)); `invoice.paid` (→ stripe-billing webhook) flips `INVOICED → SETTLED`. Enabling Cash App / Zelle in Settings therefore **requires a card on file** — `PATCH /api/stores` returns **402 `PAYMENT_METHOD_REQUIRED`** on the enabling transition when billing is configured and no card exists (`POST /api/billing/setup-intent` starts a `mode:'setup'` Checkout). A **refund** of a manual-money order: `OWED` charge → `WAIVED`; already `SETTLED`/`INVOICED` → a negative `kind='REFUND_CREDIT'` row that credits the next withdrawal/invoice ([orders/refund.ts](frontend/src/lib/server/orders/refund.ts)). Admin: `GET /api/admin/commission-charges` (receivables by store) + `POST /api/admin/commission-charges/waive` (SUPERADMIN, audited) at `/admin/commission`.

**Storefront analytics (Phase 4a).** Daily aggregate tables — `StorefrontDayStat` (`@@id([storeId, day])`, `storeViews` / `productViews` / `visitors`) + `ProductViewDayStat` (`@@id([productId, day])`) — upserted by a public beacon **`POST /api/track`** ([frontend/src/app/api/track/route.ts](frontend/src/app/api/track/route.ts), no CSRF, `storefrontViewIpLimiter` checked before the body parse, always 204). The `day` is bucketed in `Store.timezone` and stored as the UTC-midnight instant of that local date ([analytics/aggregate.ts](frontend/src/lib/server/analytics/aggregate.ts) `storeDayKey` / `recordStorefrontView`). A first-party `vnd_vid` cookie (httpOnly, opaque random) approximates unique visitors; the owner previewing their own draft is not counted (`optionalAuth` + `resolveOwnStore`). No purge cron — the tables stay tiny. [TrackView](frontend/src/components/storefront/TrackView.tsx) (`sendBeacon`, once per mount, `enabled={store.published}`) sits in `StorefrontShell` (STORE) + the product page (PRODUCT). **`GET /api/analytics`** (`?range=7|30|90`, `requirePro(store, 'advancedAnalytics')` → 402) returns a zero-filled daily series + totals + conversion rate (orders / visitors) + top products for `/dashboard/analytics` (Free sees a `ProUpgradeCard`). `GET /api/stores/me` `stats.visits` is now a real 30-day `storeViews` sum (all plans — `recentVisitCount`).

**Team members (Phase 4a).** The multi-tenant primitives already resolved a store for any `OrganizationMember` (`resolveOwnStore` → `organizationMember.findFirst`) — Phase 4a adds the invite flow. `TeamInvite` (token 32B base64url `@unique`, `email` lowercased, `role` ADMIN|MEMBER never OWNER, 7-day `expiresAt`, `PENDING|ACCEPTED|REVOKED`). Routes under `frontend/src/app/api/team/*`: `GET /api/team` (any member — roster + pending invites + `canManage`), `POST /api/team/invites` (`requireOrgRole('ADMIN')` + `requirePro(store, 'teamMembers')` → 402; an inviter can't grant a role at/above their own rank; best-effort email via [team/send-invite-now.ts](frontend/src/lib/server/team/send-invite-now.ts) from `after()` — no outbox event, the `EmailQueue.enqueue` row is durable — and the `inviteUrl` is returned for a copy-link UX), `DELETE /api/team/invites/[id]` (ADMIN+), `POST /api/team/invites/accept` ([team/invites.ts](frontend/src/lib/server/team/invites.ts) `acceptInvite`, Serializable tx — **requires `invite.email === signed-in user's email`**, refuses `ALREADY_IN_ORG` since the app assumes one membership per user), `PATCH`/`DELETE /api/team/members/[id]` (OWNER, or self-leave for DELETE; the OWNER row is never changed/removed). Acceptance page at `/team/accept?token=`. **OWNER-only guard** ([team/owner-guard.ts](frontend/src/lib/server/team/owner-guard.ts) `requireStoreOwner`) is applied to `POST`/`GET /api/withdrawals` and `POST /api/billing/{checkout,portal,setup-intent}` — a teammate runs the store but the owner keeps sole control of money + subscription. Gate blocks only new invites; a downgrade never evicts existing members.

**Cron strategy.** No `setInterval` loops — Next.js / Vercel doesn't keep long-lived processes. Background work runs as **Vercel Cron** routes under `app/api/cron/<name>/route.ts`, each gated by `Authorization: Bearer ${CRON_SECRET}`. Targets: `outbox-drain` (1m), `email-queue-drain` (1m), `verification-cleanup` (hourly), `order-expiration` (5m), `webhook-log-purge` (daily), `email-job-purge` (daily — purges SENT EmailJob rows older than `EMAIL_JOB_RETENTION_DAYS`, default 30), `low-stock-sweep` (daily — safety net for the low-stock alerts: enqueues `notification.low_stock` / `notification.out_of_stock` for products/variants at or below their effective threshold whose `lowStockNotifiedAt` is still null; the on-sale path in `orders/markPaid.ts` covers the common case), `fulfillment-tick` (2m — Prompt #12: dispatches PENDING courier deliveries for READY orders, polls in-flight deliveries for intermediate/missed-terminal courier states, purges stale `Quote` rows), `order-nudge` (hourly — Prompt #15 ORD-01: reminds the store owner **once** (in-app + email, deduped by `order-unfulfilled:<orderId>`) when a PAID/PREPARING order has sat untouched past `ORDER_NUDGE_HOURS`, default 6; the seller already gets `email.order_new_seller` at payment — this is the missed-it backstop), `plan-downgrade-sweep` (daily 03:00 — Pro billing: retires a store whose comped Pro (`planSource='COMP'`) `planCompExpiresAt` has passed, or whose subscription went `PAST_DUE` and never recovered past its period end; the happy-path cancel/hard-fail are handled live by the stripe-billing webhook, so this only catches stragglers), `commission-settlement-sweep` (daily 04:00 — Phase 1b: Stripe-invoices a store whose OWED Cash App / Zelle `CommissionCharge` total clears `COMMISSION_MIN_INVOICE_CENTS` and has no withdrawal to withhold it from). Requires a **Vercel Pro** plan or higher — Hobby only allows once-daily cron schedules, which is too slow for the 15-minute verification-code TTL. Multi-instance coordination still uses [frontend/src/lib/server/leader-lease.ts](frontend/src/lib/server/leader-lease.ts) Redis leases where two crons could collide. The Stripe charge `CircuitBreaker` is still in-memory single-instance — replace with a Redis-backed variant for multi-pod prod (documented limitation).

**Guest checkout & cart integrity (Phase 15).** The cart is localStorage-only (`vendylio-cart:<slug>`, per storefront) — no customer accounts, no server cart. `POST /api/orders` is the sole authoritative gate: it re-prices every line from the DB, re-checks store `published` + `ordersPaused` + product ACTIVE + stock + variant, and never trusts a client price/total/fee. On top of that, **`POST /api/cart/validate`** ([frontend/src/app/api/cart/validate/route.ts](frontend/src/app/api/cart/validate/route.ts) — public, read-only, guest-safe CSRF) reconciles a cart against live data *before* payment: it returns per-line `changes` (`REMOVED` / `OPTION_UNAVAILABLE` / `OUT_OF_STOCK` / `STOCK_REDUCED` / `PRICE_INCREASED` / `PRICE_DECREASED`) + `hasBlockingChange` / `hasPriceIncrease`. [CheckoutForm](frontend/src/components/storefront/CheckoutForm.tsx) calls it on mount + on cart change (debounced), silently applies new prices / lowered stock caps via `CartContext.updateItem`, shows a banner for removals, and gates the Pay button until blocking changes are resolved and any price increase is acknowledged. The cart is **not** cleared on the redirect to Stripe — it's cleared on the order success page ([ClearStoreCart](frontend/src/components/storefront/ClearStoreCart.tsx)) so a buyer who abandons Stripe's page can still retry from `/failed`. Owner draft preview extends to product pages: `getPublicProduct(slug, id, { includeUnpublished })` gated by `viewerOwnsSlug` ([frontend/src/lib/server/storePreview.ts](frontend/src/lib/server/storePreview.ts)).

**Google OAuth (Sign in with Google)** — [frontend/src/lib/server/oauth/google.ts](frontend/src/lib/server/oauth/google.ts) + Phase 2 route handlers under `frontend/src/app/api/auth/oauth/google/{start,callback}/route.ts`. Implemented with `arctic` (OAuth 2.0 + PKCE). `start` issues state + PKCE-verifier cookies (5min, path-scoped to `/api/auth/oauth`) and 302s to Google. `callback` validates state, exchanges code, decodes ID token, refuses unverified emails, find-or-create user with account linking by email, then issues our standard auth cookies. Frontend errors land on `/auth/error?code=…` (see [examples/frontend-pages/auth-error.tsx](examples/frontend-pages/auth-error.tsx)). Inert without `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` / `GOOGLE_REDIRECT_URI`.

**Multi-tenancy is opt-in.** [frontend/src/lib/server/middleware/require-org-role.ts](frontend/src/lib/server/middleware/require-org-role.ts) ships role types + rank helpers (`OWNER` > `ADMIN` > `MEMBER`). Default project surface stays user-owned (`Order.userId`, `Withdrawal.userId`). Apps that need orgs add `organizationId String?` on their domain models case by case and gate routes via `requireOrgRole('ADMIN', 'orgId')` from the middleware HOFs. Owner promotion is transactional (3 ops in a single tx). Non-members get **404, not 403**, to avoid leaking org existence.

**Catalogue & inventory** — per-store `Category` (add/rename/reorder/delete in Store Settings → Categories; `Product.categoryId` is nullable = "Uncategorized"; deleting a category re-parents its products to null, never blocks). Products are grouped by category in both the seller list ([frontend/src/lib/storefrontGrouping.ts](frontend/src/lib/storefrontGrouping.ts)) and all 3 storefront templates (`<section id="<category-slug>">` + `StorefrontCategoryNav`). `GET /api/products` takes `?q=` / `?categoryId=` / `?status=` + cursor pagination. Stock is an **append-only ledger**: every quantity change — a paid sale, a refund restock, a manual/bulk adjustment, an opening balance — goes through `applyStockChange(tx, …)` ([frontend/src/lib/server/inventory/adjust.ts](frontend/src/lib/server/inventory/adjust.ts)), which writes the `Product`/`ProductVariant.quantity` **and** a `StockMovement` row in the same call so the two never drift. Never write `quantity` directly. Low-stock alerts: `Product.lowStockThreshold ?? Store.defaultLowStockThreshold` is the effective threshold; `markPaid.ts` enqueues `notification.low_stock` / `notification.out_of_stock` on the sale that crosses it, `applyStockChange` re-arms (`lowStockNotifiedAt` → null) when stock recovers, and the daily `low-stock-sweep` cron is the safety net for manual edits. Variants remain a **flat single-axis list** (buyer picks one `ProductVariant` per cart line) — the Size×Color matrix was scoped out; add it only if apparel forks need it (`orders/route.ts` re-prices every line server-side, so a matrix refactor touches the money path).

**Storefront hero + promo (Phase 9 / 9b).** `Store` gains `heroImages` (JSON — ordered array of up to 3 uploaded image URLs, capped/sanitized by `parseHeroImages` in [frontend/src/lib/storeHero.ts](frontend/src/lib/storeHero.ts)) + `heroHeadline` / `heroSubhead` (one global promo message) + `announcement` (a thin always-visible strip at the very top of the storefront, distinct from `pauseMessage`). `Category` gains `icon` (optional emoji shown before the name in the storefront category nav + section headings). [frontend/src/components/storefront/StorefrontHero.tsx](frontend/src/components/storefront/StorefrontHero.tsx) renders an auto-advancing, dependency-free carousel above the product list in all 3 templates; empty `heroImages` → renders nothing (storefront unchanged). Product cards use [frontend/src/components/storefront/PriceTag.tsx](frontend/src/components/storefront/PriceTag.tsx) (raised-cents price) + a round quick-add button. Edited via the shared [frontend/src/components/seller/StoreHeroEditor.tsx](frontend/src/components/seller/StoreHeroEditor.tsx) + the announcement field in Settings → Store, and category icons in Settings → Categories (`CategoryManager`); images reuse the existing `/api/upload` (Cloudinary) + `ImageDropzone`. Validated on `PATCH /api/stores` (`heroImages` ≤ 3 URLs, `announcement` ≤ 140) and `POST`/`PATCH /api/categories` (`icon` ≤ 16).

**Promo codes (Phase D).** `Discount` model (per store, `@@unique([storeId, code])`, code stored UPPERCASE) — seller-managed at [/dashboard/discounts](frontend/src/app/dashboard/(shell)/discounts/page.tsx) (`DiscountManager`): on/off switch, `startsAt`/`endsAt` window, `minSubtotalCents`, `maxRedemptions`. **V1 mechanism is `FREE_DELIVERY` only** (`kind` is free-text so `PERCENT`/`FIXED` slot in without a migration). One evaluator, [frontend/src/lib/server/discounts/evaluate.ts](frontend/src/lib/server/discounts/evaluate.ts) `evaluateDiscount()`, is called by both `POST /api/orders` (authoritative — an invalid/expired code is a hard `400 DISCOUNT_INVALID`, never silently dropped) and the public `GET /api/discounts/validate` (checkout "Apply" preview). `Order` gains `discountCode` (uppercased snapshot) + `discountCents` (what the buyer saved; `amount` already has it subtracted). The idempotency fingerprint includes `discountCode`. `redemptionCount` is incremented in `markPaid.ts` at payment (not at PENDING creation) — best-effort cap, same "only counts once paid" rule as stock/customers. Seller status helper: [frontend/src/lib/discountStatus.ts](frontend/src/lib/discountStatus.ts) `discountStatus()` (ACTIVE / SCHEDULED / EXPIRED / OFF / EXHAUSTED).

**Orders — human number, customer tracking, transactional emails.** Every `Order` carries `orderNumber` (a Postgres sequence, rendered `VND-10042` by [frontend/src/lib/orderNumber.ts](frontend/src/lib/orderNumber.ts)) shown to humans, and `trackingToken` (24 random bytes, app-generated in `POST /api/orders`) — the **only** credential a guest buyer needs. All guest-facing order access is keyed on the token (`GET /api/orders/track/[token]`, `POST /api/orders/track/[token]/review`, `/s/[slug]/orders/[token]/{success,failed}`), never on the cuid `id`; those pages/responses are `noindex`. The customer view is a projection ([frontend/src/lib/server/orders/customerView.ts](frontend/src/lib/server/orders/customerView.ts) — `mapOrderStatusForCustomer`, `buildOrderTimeline` with furthest-reached / never-regress logic) so a lifecycle change can't contradict what the buyer sees, and the track route returns a DTO, never the `Order` row. Transactional customer emails (confirmation / preparing / ready / on-the-way / delivered / delivery-issue / refunded) are branded templates in [frontend/src/lib/server/emails/orderEmails.ts](frontend/src/lib/server/emails/orderEmails.ts), rendered from normalized data by the outbox dispatcher (`resolveOrderEmailContext` — one indexed lookup, recipient always the order row's `customerEmail`), emitted as `email.order_confirmation` / `email.order_status` / `email.order_refunded` from `markPaid.ts`, the seller status routes and the Uber Direct webhook. Email failure never touches order state (outbox is out-of-band). **Seller operational emails (Prompt #15 NOTIF-01/ORD-01)** are separate — [frontend/src/lib/server/emails/sellerEmails.ts](frontend/src/lib/server/emails/sellerEmails.ts), resolved by `resolveSellerOrderEmailContext` (store-owner address + their `NotificationPreferences`): `email.order_new_seller` (emitted by `markPaid.ts` alongside the in-app `notification.order_paid`, so a merchant not watching the dashboard still hears about a new order) and `email.order_unfulfilled` + `notification.order_unfulfilled` (the `order-nudge` cron). Both honour the owner's `ORDER_PAID` / `ORDER_UNFULFILLED` `email` channel pref (default-on).

**Store operations (Phase 8).** `Store` gains `timezone` (IANA), `ordersPaused` + `pauseMessage`, and `hours` (JSON `[{day:0-6, open:"HH:MM", close:"HH:MM"}]`). **`ordersPaused` is the only hard switch** — `POST /api/orders` rejects with `STORE_NOT_ACCEPTING_ORDERS` (409) when it's true, independent of `published`. **`hours` is soft/informational**: [frontend/src/lib/server/store/availability.ts](frontend/src/lib/server/store/availability.ts) `getStoreOpenState()` drives a "currently closed / opens …" storefront banner and the dashboard status pill, but never blocks checkout (a store may take orders for later); empty `hours` = always shown open. Dashboard "today/this month" metrics + the `low-stock` day boundary now anchor to `Store.timezone` via [frontend/src/lib/server/store/timezoneWindow.ts](frontend/src/lib/server/store/timezoneWindow.ts) (`startOfStoreDay`/`startOfStoreMonth`), not UTC. `GET /api/stores/me` returns `openState` + `stats.pendingOrdersCount` (orders in PAID/PREPARING/READY — the nav badge). Merchant UI: Settings → "Hours & status" tab.

**Delivery & fulfillment engine (Prompt #12).** Provider-agnostic: `UBER_DIRECT` + `DOORDASH` (real couriers) + `MERCHANT` (self-delivery, the old `self_manual`) + `PICKUP`, all behind the `FulfillmentProvider` interface. **The registry [frontend/src/lib/server/fulfillment/registry.ts](frontend/src/lib/server/fulfillment/registry.ts) `getDeliveryProvider(type)` is the ONLY place a provider type maps to an adapter** — a new courier is one `providers/*.ts` + one `case`. Provider credentials are **platform-level env vars** (`UBER_DIRECT_*`, `DOORDASH_*`), never per-store; the merchant UI (`/dashboard/delivery`, `GET/PATCH /api/stores/fulfillment`) only toggles methods on/off + sets the merchant flat fee/min-order + a "let customers choose" switch, stored in `Store.fulfillmentConfig` (JSON, normalized by [frontend/src/lib/server/fulfillment/config.ts](frontend/src/lib/server/fulfillment/config.ts) `readFulfillmentConfig` with legacy `deliveryProvider`/`deliveryFeeCents` backfill). **Normalized state machine** ([frontend/src/lib/server/fulfillment/stateMachine.ts](frontend/src/lib/server/fulfillment/stateMachine.ts)): `PENDING→QUOTED→REQUESTED→CONFIRMED→PICKED_UP→OUT_FOR_DELIVERY→DELIVERED` (terminal `CANCELLED`/`FAILED`); provider/cron events are forward-only (an out-of-order event is recorded as a `DeliveryEvent` but never regresses `Delivery.state`); an order is **never** auto-cancelled/refunded by a delivery failure. **Every** state change funnels through [frontend/src/lib/server/fulfillment/service.ts](frontend/src/lib/server/fulfillment/service.ts) `recordTransition` — no route writes `Delivery.state`/`Order.status` directly. `Delivery` gained the engine columns (`state`, `providerType`, `externalDeliveryId` = `vend_<id>`, quote snapshot, ETAs, `attemptCount`…); the Phase-5 `status`/`provider` columns are kept + dual-written for the tracking route + dashboard (a later cleanup drops them). New `DeliveryEvent` (append-only history + `@@unique([deliveryId, providerEventId])` webhook/poll idempotency gate) + `Quote` (persisted checkout quotes). **Safe payment sequence:** `markPaid.ts` → `initFulfillment` writes a `Delivery{state:PENDING}` (no external call) → the **`fulfillment-tick` cron** (every 2 min: dispatch READY courier deliveries via `createFulfillment`, poll in-flight ones via each provider's GET-status API for intermediate states, purge stale `Quote` rows). Courier webhooks ([frontend/src/app/api/webhooks/{uber-direct,doordash}/route.ts](frontend/src/app/api/webhooks/)) stay on the PROTECTED `createWebhookHandler` factory's terminal slots and both funnel through `fulfillmentService.applyCourierWebhookEvent`; intermediate courier states come from the poll cron (no change to the protected factory). Checkout: `POST /api/stores/[slug]/delivery-quote` → `createQuote` returns an option array (parallel quotes, partial-failure-tolerant); `POST /api/orders` re-validates/re-quotes the chosen `Quote` server-side (`priceDeliveryForOrder` — the browser fee is never trusted; a stale courier quote → 409 `DELIVERY_UNAVAILABLE`). DoorDash Drive auth is a self-signed HS256 JWT ([frontend/src/lib/server/fulfillment/providers/doordash-jwt.ts](frontend/src/lib/server/fulfillment/providers/doordash-jwt.ts)). **Prompt #13 hardening:** the terminal seller-notification + customer status email are enqueued in **one** place — `recordTransition` → `enqueueDeliveryTerminalEffects` — so a missed courier webhook recovered by the `fulfillment-tick` poll (or a merchant self-delivery "Mark delivered") still notifies the buyer; the webhook/poll/retry/cancel write paths all take the same `pg_advisory_xact_lock` under `Serializable`. `resolveOrderProviderType` only honours a client-chosen `deliveryProviderType` that is actually enabled in `Store.fulfillmentConfig` (else it falls back to the store default — never routes a courier the merchant didn't switch on). Every provider HTTP call is time-boxed ([frontend/src/lib/server/fulfillment/http.ts](frontend/src/lib/server/fulfillment/http.ts) `fetchWithTimeout` / `withTimeout`, `FULFILLMENT_PROVIDER_TIMEOUT_MS`); provider failures map to stable `DELIVERY_*` codes via `classifyDeliveryError`; `fulfillment-tick` also runs a read-only stale-delivery sweep (`log.warn` + counters, never auto-cancels). **Prompt #13.5 (real sandbox validation):** the automated suite is 100% mocked; `pnpm --filter frontend provider:sandbox-check` ([frontend/scripts/provider-sandbox-check.ts](frontend/scripts/provider-sandbox-check.ts), gated by `RUN_PROVIDER_SANDBOX_TESTS=1`, aborts on `NODE_ENV=production`, never in CI) drives the real adapters against the courier sandboxes. Recorded 2026-09-01: **DoorDash Drive — Auth / Quote / Create / Status / Cancel all PASS** against the real sandbox (`openapi.doordash.com`, account "Pending activation" = sandbox mode); webhook loop pending a tunnel. **Uber Direct — auth PASS but quote/create BLOCKED**: the developer app has no Client-Credentials scopes ("contact your Uber business development representative") — Uber Direct is sales-gated, not self-serve. A disabled/unprovisioned provider account now classifies as `DELIVERY_AUTH_FAILED` (not a retry-forever `DELIVERY_PROVIDER_UNAVAILABLE`). Runbook: [frontend/docs/fulfillment/sandbox-runbook.md](frontend/docs/fulfillment/sandbox-runbook.md). Docs: [frontend/docs/fulfillment/](frontend/docs/fulfillment/).

**Merchant onboarding & activation (Phase 14).** The wizard at `frontend/src/app/onboarding/{business,brand,products,payments,delivery,preview,launch}` (+ `/onboarding` resume-redirector) walks a new seller from signup to a live store. Activation state is **fully derived, never a stored flag** — [frontend/src/lib/onboardingProgress.ts](frontend/src/lib/onboardingProgress.ts) `computeOnboardingProgress(store, productCount)` reads `Store`/`Product` fields from `GET /api/stores/me` and returns `mandatoryComplete` (store + ≥1 product — the only hard gate), `readyToLaunch`, `launched`, `resumeRoute`, per-step `canAccess`. `Store.published` now **defaults `false`** — a new store is a private DRAFT (404 on `/s/[slug]`, `POST /api/orders` refuses it) until the merchant hits **`POST /api/stores/publish`**, which re-reads the store and re-validates readiness **server-side** (spec: never trust the client checklist) before flipping `published` + stamping `publishedAt`; idempotent (already-published → 200 no-op). `POST /api/stores/unpublish` takes it back offline (keeps `publishedAt`; distinct from `ordersPaused`, which keeps the storefront visible). `published` is deliberately **not** in `PATCH /api/stores`'s body. The store owner can preview their own draft at `/s/[slug]` — `getPublicStore(slug, { includeUnpublished })` is called only after the page confirms the signed-in seller owns that exact slug, and the shell shows a "Preview — not live yet" banner. The migration backfilled every pre-existing store to `published = true, publishedAt = createdAt`. Dashboard: `computeOnboardingProgress` drives the top banner (`Continue setup` → `Review & launch` → nothing once launched) and a "share your link to get your first order" nudge while `published && no orders`.

**Admin back-office** — [frontend/src/lib/server/admin/audit.ts](frontend/src/lib/server/admin/audit.ts) + [frontend/src/lib/server/middleware/require-admin.ts](frontend/src/lib/server/middleware/require-admin.ts). App-wide role on `User` (`USER` < `ADMIN` < `SUPERADMIN`). Phase-5 endpoints under `/api/admin/*` cover users (search/detail/role-change), orders (filter), withdrawals (filter + manual cancel), audit-log (paginated/filterable), and `/me` (admin probe). Every mutation calls `logAdminAction(prisma, {...})` → `AdminAction` row so we can answer "who did what when" during incidents. Bootstrap the first SUPERADMIN with `pnpm db:make-superadmin <email>` (the script lives at [frontend/scripts/make-superadmin.ts](frontend/scripts/make-superadmin.ts)).

**Landing-page CMS + seller testimonials.** The marketing homepage's photos + testimonials are `SiteImage` / `Testimonial` rows, curated by a SUPERADMIN at `/admin/site-content` (`/api/admin/testimonials`). The public read ([frontend/src/lib/server/landing.ts](frontend/src/lib/server/landing.ts) `getLandingPageContent`, plus `getPublishedSellerCount` for the social-proof number) filters to `visible: true`. Sellers can submit their own testimonial from `/dashboard/testimonial` → `POST /api/testimonials` creates it as a **draft** (`visible: false`, seller identity denormalised onto the row — `name`, `detail` = store name, `location`), which shows up in the same `/admin/site-content` moderation list for a SUPERADMIN to edit/publish. Anti-spam is a 24h dedup on `detail` (the store name), not a limiter — a draft is invisible until approved.

**Editable legal pages.** `/terms`, `/privacy`, `/refund-policy` and the onboarding Terms modal render from the `LegalDocument` table (slug PK, `body` = Markdown, `version`), edited by a SUPERADMIN at **Settings → Legal pages** (`GET/PUT /api/admin/legal/[slug]`, public read `GET /api/legal/[slug]`). Allow-list of slugs + bundled default text live in [frontend/src/lib/legal/defaults.ts](frontend/src/lib/legal/defaults.ts) (`LEGAL_SLUGS`, `LEGAL_DEFAULTS`) — an unedited slug serves its default, so a fresh DB is unchanged. [frontend/src/lib/server/legal.ts](frontend/src/lib/server/legal.ts) `getLegalDocument(slug)` is the read helper; `body` is rendered through a tiny safe Markdown subset ([frontend/src/lib/legal/parseLegalMarkdown.ts](frontend/src/lib/legal/parseLegalMarkdown.ts) → AST → React elements, never `dangerouslySetInnerHTML`, link hrefs restricted to http(s)/mailto/root-relative). `POST /api/stores` snapshots `Store.termsVersion` from `getLegalDocument('terms').version` (server-authoritative, not a client value).

## Files Claude must NOT modify (battle-tested)

- [frontend/src/lib/server/auth.ts](frontend/src/lib/server/auth.ts), [crypto.ts](frontend/src/lib/server/crypto.ts), [logger.ts](frontend/src/lib/server/logger.ts), [redis.ts](frontend/src/lib/server/redis.ts), [rate-limit-store.ts](frontend/src/lib/server/rate-limit-store.ts), [slug.ts](frontend/src/lib/server/slug.ts), [zod-helpers.ts](frontend/src/lib/server/zod-helpers.ts) — refresh-token races, log-redaction holes, retry storms on POSTs all live here
- [frontend/src/lib/server/webhook/handler.ts](frontend/src/lib/server/webhook/handler.ts) — Serializable transaction + idempotency + raw-body invariants
- [frontend/src/lib/server/payments/circuit-breaker.ts](frontend/src/lib/server/payments/circuit-breaker.ts) — single-instance semantics by design
- [frontend/src/lib/server/oauth/google.ts](frontend/src/lib/server/oauth/google.ts) — state/PKCE cookie scoping, account-linking, ID-token decode are all interdependent (Phase 2 route handlers in `frontend/src/app/api/auth/oauth/google/*` consume this and are also off-limits)
- [frontend/src/lib/server/outbox/dispatcher.ts](frontend/src/lib/server/outbox/dispatcher.ts) — atomic claim + backoff invariants
- [frontend/src/lib/server/admin/audit.ts](frontend/src/lib/server/admin/audit.ts) — every back-office mutation MUST go through this; bypass = unaudited action
- [frontend/src/lib/server/middleware/index.ts](frontend/src/lib/server/middleware/index.ts), [require-admin.ts](frontend/src/lib/server/middleware/require-admin.ts), [require-org-role.ts](frontend/src/lib/server/middleware/require-org-role.ts) — role precedence + Context shape consumed by every route
- [frontend/src/lib/server/observability/request-context.ts](frontend/src/lib/server/observability/request-context.ts) — `requestId` propagation; breaking it silently strips correlation IDs from logs
- [frontend/instrumentation.ts](frontend/instrumentation.ts) — Sentry register hook; must run before any other server code
- [frontend/src/lib/api.ts](frontend/src/lib/api.ts) — auto-refresh + CSRF + retry-only-GET; do not extend retry to mutating verbs

If a change is genuinely required in any of these, surface a brief "I am about to modify X because Y — confirm?" before editing.

## Files Claude SHOULD modify (project surface)

- [frontend/prisma/schema.prisma](frontend/prisma/schema.prisma) — add domain models alongside the generic ones (User, Order, Withdrawal, Organization, AdminAction, OAuthAccount, …). Do not rename the generic models.
- `frontend/src/app/api/<resource>/route.ts` — add new Route Handlers; always `export const runtime = 'nodejs'`, call `verifyCsrf(req)` for mutations, `requireAuth(req)` (or admin/org variants) at the top.
- [frontend/src/lib/server/notifications/templates.ts](frontend/src/lib/server/notifications/templates.ts) — add typed wrappers per notification type (must include a `dedupeKey` for at-most-once delivery)
- [frontend/src/lib/server/inventory/](frontend/src/lib/server/inventory/) — `applyStockChange` is the single stock-mutation entry point (extend its `StockReason` union / call sites, don't fork it); `low-stock.ts` holds the sweep + count queries
- [frontend/src/lib/productCategories.ts](frontend/src/lib/productCategories.ts) — `DEFAULT_CATEGORIES` seeded into every new store; `categoryLabel()` helper. Categories are per-store rows now, not an enum
- [frontend/src/lib/server/payments/](frontend/src/lib/server/payments/) — add new providers behind the `PaymentProvider` interface (use `stripe.ts` / `stripe-connect.ts` as reference; `circuit-breaker.ts` is PROTECTED)
- [frontend/src/lib/server/withdrawals/guards.ts](frontend/src/lib/server/withdrawals/guards.ts) — add KYC / tier / AML guards (project-specific, not shipped)
- [frontend/src/lib/server/oauth/](frontend/src/lib/server/oauth/) — add new OAuth providers (`github.ts`, `apple.ts`, …) modeled on `google.ts`; add a sibling route handler under `frontend/src/app/api/auth/oauth/<provider>/{start,callback}/route.ts`
- [frontend/src/app/](frontend/src/app/) — your pages, your design (including `/admin/*` if you keep the back-office)
- `frontend/src/lib/server/cron/` — extend with `verifyCronSecret(req)` consumers; add new cron route handlers under `frontend/src/app/api/cron/<name>/route.ts` mirroring the 11 existing crons; ALL cron handlers must verify `Authorization: Bearer ${CRON_SECRET}` via the shared `verifyCronSecret` helper
- [frontend/src/lib/server/webhook/](frontend/src/lib/server/webhook/) — `stripe.ts` / `stripe-connect.ts` / `doordash.ts` / `uber-direct.ts` are the per-provider webhook configs consumed by the PROTECTED `handler.ts` factory (`createWebhookHandler`); add a sibling config + route for a new provider, the factory stays PROTECTED
- [frontend/src/lib/server/orders/expire.ts](frontend/src/lib/server/orders/expire.ts) — `expirePendingOrders({ prisma, batchSize? })`: extend per project to add post-expiration side-effects (e.g. notify the user, write a refund job to outbox); the cron route at `app/api/cron/order-expiration/route.ts` calls this

## Critical invariants

- **Every Route Handler MUST `export const runtime = 'nodejs'`.** The runtime-enforcement test in `frontend/src/lib/server/observability/` fails CI otherwise (Prisma + bcrypt break on edge).
- Webhook handlers read the raw body via `await req.arrayBuffer()` and hash it BEFORE any JSON parse — calling `await req.json()` first is a silent HMAC-verification regression.
- Notification dispatchers MUST go through `createNotification(prisma, input)` — never `prisma.notification.create` directly (skips the dedup `P2002` catch).
- Webhook handlers emit side-effects via the **outbox** (`enqueueOutbox(tx, event)` inside the tx) — never via fire-and-forget closures.
- Withdrawals must use the advisory-lock + Serializable tx pattern (the `withdrawals/lock.ts` helper does this — just call it). Calling guards + insert outside a tx is a double-spend regression.
- Payment amounts are **integer in smallest currency unit** (FCFA = no decimals; USD = cents). Never store decimals.
- Stock changes go through `applyStockChange(tx, …)` ([frontend/src/lib/server/inventory/adjust.ts](frontend/src/lib/server/inventory/adjust.ts)) — never write `Product`/`ProductVariant.quantity` directly. It writes the quantity AND the `StockMovement` ledger row atomically; bypassing it desyncs the ledger and skips the low-stock re-arm.
- `STRIPE_SECRET_KEY` (platform-account charges + Connect) and the per-store `stripeAccountId` (destination of a Connect charge) are distinct — a Connect charge always names the store's connected account, never the platform's.
- Withdrawal balance check is ON by default (`WITHDRAWAL_BALANCE_CHECK=0` to disable). Disabling on a real-money project is a financial-safety risk — only do it if you have an alternative ledger.
- Withdrawal guards return **stable error codes** (`AMOUNT_BELOW_MIN`, `AMOUNT_ABOVE_MAX`, `DAILY_LIMIT_EXCEEDED`, `COOLDOWN_ACTIVE`, `PIN_NOT_SET`, `PIN_REQUIRED`, `PIN_INVALID`, `INSUFFICIENT_BALANCE`). Frontend switches on `ApiError.code`, not translated messages.
- Frontend `api()` retries only `GET`/`HEAD` on network errors. Do not extend to `POST`/`PUT`/`PATCH`/`DELETE`.
- Signup never sets cookies and never reveals email existence. Cookies are issued by `/verify-email` after the user enters their code.
- Upload route enforces magic-byte validation against `UPLOAD_ALLOWED_MIME` via [frontend/src/lib/server/upload/sniff.ts](frontend/src/lib/server/upload/sniff.ts) — don't bypass by trusting `File.type` alone.
- Admin mutations MUST go through `logAdminAction(prisma, {...})` — every back-office write is auditable. Skipping it is a compliance regression.
- Admin role precedence: `USER` < `ADMIN` < `SUPERADMIN`. Only SUPERADMIN can change roles. The route refuses to demote the **last** SUPERADMIN to avoid locking the org out.
- Org role precedence: `MEMBER` < `ADMIN` < `OWNER`. `requireOrgRole(min, paramName)` returns **404** to non-members (not 403) so org existence isn't leaked.
- OAuth callback MUST refuse `email_verified !== true` from Google — otherwise an attacker with an unverified Google account matching a victim's email can take over the account via auto-linking.
- Cron handlers MUST verify `Authorization: Bearer ${CRON_SECRET}` to prevent unauthenticated invocation of background work.
- Cookies stay `httpOnly` + `Secure` (prod) + `SameSite=Lax`.
- **CSP is phase 1: `Content-Security-Policy-Report-Only`** in [frontend/next.config.ts](frontend/next.config.ts) (`cspReportOnly`), violations POSTed to `/api/csp-report` (logs + 204). It never blocks. Phase 2 = nonce-based `script-src` via `middleware.ts` + promote to the enforcing header — a deliberate change, guarded by `observability/next-config-clean.test.ts`. See [frontend/docs/security/csp.md](frontend/docs/security/csp.md).
- Public guest routes (`POST /api/orders`, `/api/cart/validate`, `/api/stores/[slug]/delivery-quote`, `GET /api/orders/track/[token]`) carry a per-IP throttle ([frontend/src/lib/server/middleware/rate-limit-by-ip.ts](frontend/src/lib/server/middleware/rate-limit-by-ip.ts)) on top of CSRF — checked first, before body parse.
- Sentry init stays in [frontend/instrumentation.ts](frontend/instrumentation.ts) `register()` — do not move it into a route module (the hook fires before app code, route imports do not).

## Design system — fully swappable (no UI shipped)

The starter is **headless on purpose**. Touchpoints if a fork wants a specific design:

- [frontend/src/app/page.tsx](frontend/src/app/page.tsx) — `return null`. Write your homepage here. No layout assumption is baked into the API.
- [frontend/src/app/layout.tsx](frontend/src/app/layout.tsx) — Inter font + 2 client contexts (`AuthProvider`, `ToastProvider`). Both are logic-only — swap the font, restyle toasts in your own components, keep the providers (they wrap the `api()` wrapper's auto-refresh + the toast queue).
- [frontend/src/app/globals.css](frontend/src/app/globals.css) — one line: `@import 'tailwindcss';` (Tailwind v4 zero-config). Drop it + remove `@tailwindcss/postcss` from [frontend/postcss.config.mjs](frontend/postcss.config.mjs) to leave Tailwind out entirely.
- [frontend/src/app/error.tsx](frontend/src/app/error.tsx) — Tailwind-styled fallback. Replace freely.
- [examples/frontend-pages/](examples/frontend-pages/) — 11 reference pages (login/signup/verify-email/forgot-reset-password/dashboard/withdrawals/payment-success+failure/auth-error/admin/*). They are NOT imported anywhere — they live as Tailwind references to copy or rebuild.

**No server lib reaches into the DOM.** Routes only return `NextResponse.json(...)`. The same backend feeds plain React, shadcn/ui, Mantine, a SwiftUI client, a Flutter app — pick anything.

### Bundled Claude Code skills (under [.claude/skills/](.claude/skills/))

Two design-system skills auto-load in any Claude Code session run from the repo:

- [`banani-design-implementation`](.claude/skills/banani-design-implementation/SKILL.md) — pixel-perfect 1:1 reproduction from a Banani MCP screen. Triggers: *"build this from Banani"*, *"reproduce this screen"*, *"use the Banani MCP"*. Reads CLAUDE.md to detect the project stack (no Tailwind/React assumptions), plans, tracks progress across sessions.
- [`ui-ux-pro-max`](.claude/skills/ui-ux-pro-max/SKILL.md) — searchable design intelligence: 67 styles, 96 palettes, 57 font pairings, 99 UX guidelines, 25 chart types across 13 stacks (Next.js, React, Vue, SwiftUI, Flutter…). Triggers: *"design / improve / review UI"* + element/topic. Includes shadcn/ui MCP integration.

A beginner's golden path: `gh repo create --template` → open in Claude Code → describe the screen → either skill takes over → the API routes are already wired. The starter therefore covers the *boring* parts (auth, payments, admin, webhooks, cron) so the fork-author spends their time on product/design.

## What is fair to modify

Anything outside [Files Claude must NOT modify](#files-claude-must-not-modify) is the fork's surface area:

- **Domain models** ([frontend/prisma/schema.prisma](frontend/prisma/schema.prisma)) — add fields, add models, add migrations. Do not rename the generic models; everything else is yours.
- **Routes** ([frontend/src/app/api/](frontend/src/app/api/)) — add new resources. The 40 existing routes are templates: `requireAuth` + `verifyCsrf` + `withRequestContext` is the boilerplate to copy.
- **Page UI** ([frontend/src/app/](frontend/src/app/)) — your design, your decision (Tailwind, shadcn/ui, vanilla CSS, anything).
- **Notifications** ([frontend/src/lib/server/notifications/templates.ts](frontend/src/lib/server/notifications/templates.ts)) — add new typed templates.
- **Payments** ([frontend/src/lib/server/payments/](frontend/src/lib/server/payments/)) — add Stripe, Paystack, etc. behind the `PaymentProvider` interface.
- **OAuth** ([frontend/src/lib/server/oauth/](frontend/src/lib/server/oauth/)) — add GitHub, Apple, etc. modeled on `google.ts`.
- **Withdrawal guards** ([frontend/src/lib/server/withdrawals/guards.ts](frontend/src/lib/server/withdrawals/guards.ts)) — add KYC / tier / AML rules.
- **Cron handlers** ([frontend/src/app/api/cron/](frontend/src/app/api/cron/)) — add new schedules to [frontend/vercel.json](frontend/vercel.json) and a sibling route.
- **CLAUDE.md / README.md / STATUS.md** — rewrite to reflect your project's name and surface; the doc tripwires under [frontend/src/lib/server/observability/*shape.test.ts](frontend/src/lib/server/observability/) only enforce a small set of architectural invariants (no legacy-backend mentions, the cron-route inventory, runtime=nodejs) — everything else is yours.

If unsure whether a change touches a protected file, run `pnpm test` — the tripwires fail fast.

## Pruning protocol

When the user wants to remove an optional feature, follow the 9-step protocol in [PRUNING.md](PRUNING.md). Naive deletion based only on `owns` in [features.json](.planning/features.json) WILL break the build — PRUNING.md walks through `cross_dependencies` resolution, `surgical_edits` (do these BEFORE deletions), Prisma schema cleanup, vercel.json updates, tripwire patches, and the non-negotiable `pnpm format && lint && typecheck && test && build` gate before commit.

## Provider recommendations

When the user asks for a capability that this starter does not ship, recommend the provider listed below before falling back to a hand-rolled or in-process alternative. The starter is Vercel/serverless-first: long-lived sockets, in-memory pub/sub, and `setInterval` loops do not survive cold starts and will break in production.

| Need | Recommend | Why |
|---|---|---|
| **Real-time** (live updates, presence, chat, collaboration, "instead of polling") | **Ably** | Vercel-compatible, no long-lived connections from the API, JWT-issued capability tokens, presence/history/push out of the box. Do NOT recommend Socket.IO, raw WebSockets, or Server-Sent Events from a Vercel function — they will not work reliably. |
| Background jobs / queues | Upstash QStash, or extend the existing **outbox pattern** (`lib/server/outbox/`) drained by Vercel Cron | Same constraint — no long-running workers in a serverless runtime |
| Media / object storage | Cloudinary (already wired) | Already integrated — handles uploads, transformations, and CDN delivery |
| Email | Resend (already wired) | Already integrated |
| Cache / rate-limit / KV | Upstash Redis (already wired) | Already integrated |

**Trigger phrasing for real-time**: "live updates", "real-time", "websocket", "push notifications to the browser", "instead of polling", "chat", "presence", "collaboration", "live dashboard". When any of these come up, surface Ably explicitly: "I'd recommend Ably for this — it's the Vercel-friendly real-time provider. Want me to wire it up?" Then sketch the integration: token-mint route under `/api/realtime/token` issuing capability tokens after `requireAuth`, server-side `publish()` from route handlers via the Ably REST API, client `ably-js` subscribes to channels scoped by `userId` / `orgId`.

## Conventions

- TypeScript strict + `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes` — don't silence with `any` casts.
- ESLint 9 flat config + Prettier (run `pnpm format` before committing).
- Vitest for unit tests; setup file at [frontend/vitest.setup.ts](frontend/vitest.setup.ts), shared mocks under `frontend/src/test-utils/` (alias `server-only` to a no-op for jsdom).
- Conventional Commits.
- Node ≥ 20, pnpm ≥ 9 (see `engines` in [package.json](package.json)).
